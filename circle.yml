machine:
  node:
    version: 0.10.34
database:
  post:
   - bin/resettestdb.sh
deployment:
  ci:
    branch: insp
    commands:
    - ssh youpers@insp-ci.youpers.com 'bash -s' < bin/deploy.sh insp insp-ci


