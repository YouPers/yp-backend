deployment:
  uat:
    branch: uat
    commands:
    - ssh youpers@uat.youpers.com 'bash -s' < bin/deploy.sh
  ci:
    branch: master
    commands:
    - ssh youpers@cimaster.youpers.com 'bash -s' < bin/deploy.sh


