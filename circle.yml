machine:
  node:
    version: 2.1.6
database:
  post:
   - bin/resettestdb.sh
deployment:
  ci:
    branch: insp
    commands:
    - ssh youpers@insp-ci.youpers.com 'bash -s' < bin/deploy.sh insp insp-ci


