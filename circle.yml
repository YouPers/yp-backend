machine:
  node:
    version: 4.1.1
database:
  post:
   - bin/resettestdb.sh
deployment:
  ci:
    branch: insp
    commands:
    - ssh youpers@insp-ci.youpers.com 'bash -s' < bin/deploy.sh insp insp-ci


